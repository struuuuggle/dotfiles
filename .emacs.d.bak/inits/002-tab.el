;;; 002-tab.el --- tab settings
;;; Commentary:
;;; Code:

;; タブ幅をスペース2つ分にする
(setq-default tab-width 2)

;; タブ文字ではなくスペースを使う
(setq-default indent-tabs-mode nil)

(provide '002-tab)
;;; 002-tab.el ends here
