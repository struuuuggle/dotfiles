;;; 000-face.el --- Appearance
;;; Commentary:
;;; Code:

;; カーソル行をハイライトする
;; (global-hl-line-mode t)

;;透明度
(custom-set-faces
 '(default ((t (:background "nil")))))


;; スクロールを一行ずつにする
(setq scroll-step 1)

(provide '000-face)
;;; 000-face.el ends here
