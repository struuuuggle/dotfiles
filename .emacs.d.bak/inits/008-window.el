;;; 008-window.el --- for window
;;; Commentary:
;;; Code:

;; ウィンドウサイズ
(setq default-frame-alist
      (append (list
               '(width . 117)
               '(height . 80)
               )
              default-frame-alist))

;;; 008-window.el ends here
