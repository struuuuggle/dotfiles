;;; 02-tab.el --- tab settings
;;; Commentary:
;;; Code:

;; タブ幅をスペース2つ分にする
(setq-default tab-width 2)

;; タブ文字ではなくスペースを使う
(setq-default indent-tabs-mode nil)

(provide '02-tab)
;;; 02-tab.el ends here
